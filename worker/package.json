{
    "name": "weld-staff-api",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "deploy": "node scripts/verificar-antes-de-publicar.mjs && wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "node test/bateria.mjs"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240208.0",
        "typescript": "^5.3.3",
        "wrangler": "^3.28.1"
    }
}
